# Paths to clean (reference) and modified client directories
CLEAN_CLIENT = "clients/clean"
MOD_CLIENT   = "clients/mod"

# Root output directory for all exported assets
OUTPUT_DIR = "output"

# Item IDs >= this value are considered custom/shard-specific art
CUSTOM_ART_THRESHOLD = 0x8000
